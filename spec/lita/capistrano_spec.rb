require 'spec_helper'

describe Lita::Capistrano do
  it 'has a version number' do
    expect(Lita::Capistrano::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
